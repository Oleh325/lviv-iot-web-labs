import styled from "styled-components";

export const TileContainer = styled.div`
    width: 25%;
    gap: 1rem;
    align-items: center;
    text-align: center;
    img.cat {
        width: 100%;
    }
    .tile-title {
        color: var(--darkgray);
        font-size: 2rem;
        font-weight: 600;
        padding-bottom: 1rem;
    }
    .tile-description {
        color: var(--lightgray);
    }

`